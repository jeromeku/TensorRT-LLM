MoE Call Stack Trace Docs
=========================

This folder contains a full, end‑to‑end call‑stack trace for the fused MoE path in TensorRT‑LLM’s PyTorch integration.

- See `codex/moe_callstack.md` for the main document.
- All file references are workspace‑relative with `path:line` anchors so they are clickable in VSCode.

